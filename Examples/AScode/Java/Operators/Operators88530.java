class Java {
    public static void main(String[] args) {
  
      int month = 0;
      String result;
  
      // ternary operator
      result = (month == 0) ? "Jan." : "Not Jan";
      System.out.println(result);
      // Jan.
    }
  }
